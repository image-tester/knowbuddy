%h2 Forgot your password?
= form_for(resource, as: resource_name, url: password_path(resource_name), html: {id: "formID", method: :post }) do |f|
  = devise_error_messages!
  %div
    = f.label :email
    %br/
    = f.email_field :email, class: "validate[required, custom[email]]"
  %div= f.submit "Send me reset password instructions"
= render partial: "devise/shared/links"

